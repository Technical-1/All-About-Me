# local-budget-manager — stack

State: `planned`. No stack yet. Per its owning plan: a **Node** backend on the tower owning SQLite and every calculation, plus a web frontend - A9 does not bind this domain shell.
